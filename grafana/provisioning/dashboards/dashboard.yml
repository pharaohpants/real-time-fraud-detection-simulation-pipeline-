apiVersion: 1

providers:
  - name: fraud-pipeline
    orgId: 1
    folder: Fraud Pipeline
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
